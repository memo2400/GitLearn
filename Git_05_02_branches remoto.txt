
Despues de clonar el proyecto escribe el siguiente comando

git branch -a
* main
  remotes/origin/HEAD -> origin/main
  remotes/origin/rama1
  remotes/origin/rama2
  remotes/origin/rama3
  remotes/origin/main

Te mostrara todas las ramas de ese repositorio y despues has un checkout por cada rama.

git checkout rama1
Switched to branch 'rama1'

Tendras las ramas que haces checkout en tu repositorio local

git branch
* rama1
  main

#############################################################

Subir rama nueva con push

$ git push origin mi-rama-nueva


Si mas adelante solo se quiere hacer simplemente git push sin especificar rama:

$ git push --set-upstream origin mi-rama-nueva

#############################################################

Para ver las ramas con su remote asociado
$ git branch -vv

CASO 1
Crear la rama remota correcta (si aún no existe)
Sube rama local al remoto
$ git push -u origin RamaNueva

CASO 2
Si la rama ya existe y quieres forzar el seguimiento
$ git branch --set-upstream-to=origin/desarrollo desarrollo

FINAL
Revisar las ramas con su remote asociado
$ git branch -vv



